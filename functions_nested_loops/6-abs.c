#include <stdio.h>
#include "main.h"

/**
 * _abs - begining
 *
 * Description: print if negative or positive
 *
 * @n: is input
 *
 * Return: 0 ends the program
 */

int _abs(int, n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
